// This file centralizes the API configuration for the entire frontend application.
// By defining the backend server's address here, we can easily change it later
// if we deploy the backend to a different URL, without having to update every component.

export const API_BASE_URL = 'http://localhost:5000';
